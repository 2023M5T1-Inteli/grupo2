let coordinates = [];

for(let i = 0; i < 518399; i++) {

    coordinates.push({

        altitude: parseInt(Math.random() * 10000),
        longitude: Math.random() * 180,
        latitude: Math.random() * 90
        
    })
    
}